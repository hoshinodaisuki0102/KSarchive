# KSarchive 배포/PWA 운영 가이드

## 핵심 결론
친구들에게 ZIP 파일을 뿌리는 방식은 업데이트가 자동 반영되지 않습니다.
업데이트를 바로바로 적용하려면 한 곳에 배포한 웹 주소를 친구들에게 보내고, 친구들은 그 주소를 스마트폰에서 `홈 화면에 추가`해야 합니다.

권장 방식:

1. GitHub 저장소에 프로젝트 업로드
2. Vercel, Netlify, Render 중 하나에 배포
3. 친구들에게 배포 URL 공유
4. 친구들은 크롬/사파리에서 접속 후 홈 화면에 추가
5. 내가 코드를 수정해서 다시 배포하면 친구들이 설치한 PWA에도 최신 화면이 반영됨

## 관리자 계정
관리자는 일반 로그인 화면에서도 바로 로그인할 수 있고, `/admin` 승인실에도 들어갈 수 있습니다.

관리자 계정은 `.env.local` 또는 배포 서비스의 Environment Variables에서 바꿉니다.

```env
KS_ADMIN_USERNAME=ksadmin
KS_ADMIN_PASSWORD=Ksa!0629_Admin
```

배포 전에는 반드시 다른 값으로 바꾸는 것을 권장합니다.

## 중요한 보안 주의
프로젝트 ZIP을 친구들에게 그대로 보내면 `.env.local`에 들어 있는 API 키와 관리자 비밀번호가 노출될 수 있습니다.
친구들에게는 ZIP이 아니라 배포 URL만 보내는 것이 안전합니다.

## PWA 업데이트 방식
현재 PWA는 새 버전이 올라오면 서비스 워커가 1분 간격으로 업데이트를 확인합니다.
새 버전이 감지되면 앱을 다시 열거나 새로고침했을 때 최신 화면이 반영됩니다.

단, 배포 서버에 최신 코드가 올라가 있어야 합니다.
로컬 컴퓨터에서만 파일을 수정하면 친구들 앱에는 반영되지 않습니다.

## 회원 데이터 저장 주의
현재 가입 요청은 `data/users.json`에 저장됩니다.
로컬 서버나 일반 Node 서버에서는 작동하지만, Vercel 같은 서버리스 환경에서는 파일 저장이 영구 유지되지 않을 수 있습니다.
친구들과 실제로 오래 쓸 거면 Supabase/Firebase 같은 DB 연결을 권장합니다.
